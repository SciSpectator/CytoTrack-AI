# Optional dependencies for the NVIDIA LocateAnything-3B detection backend
# (CellDetector sensitivity="locate"). This backend is optional and is not
# used by the default research-paper result path because it carries a
# non-commercial model license. If these packages or the model weights are
# unavailable, the app automatically falls back to the open classical
# multi-strategy detector.
#
# Notes:
#   * Large model (~3B params). A CUDA GPU is strongly recommended; CPU
#     inference works but is very slow.
#   * LICENSE: NVIDIA non-commercial — academic / non-profit research use
#     only. Do not use for commercial purposes.
#   * torch / torchvision come from requirements.txt.
transformers==4.57.1
peft
decord
lmdb
# Optional prompt optimizer for LocateAnything cell queries. It is used only
# when an LM API key is configured; otherwise the deterministic microscopy
# prompt is used.
dspy-ai
